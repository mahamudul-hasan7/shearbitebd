import { AddressManager } from "@/features/donor-account/address-manager";

export const metadata = { title: "Saved Addresses | ShareBite BD" };

export default function DonorAddressesPage() {
  return <AddressManager />;
}
