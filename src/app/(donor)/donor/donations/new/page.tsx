import { DonationWizard } from "@/features/donations/donation-wizard";

export const metadata = { title: "Add Surplus Food" };

export default function NewDonationPage() {
  return <DonationWizard />;
}
