import { NotificationCenter } from "@/features/donor-account/notification-center";

export const metadata = { title: "Notifications | ShareBite BD" };

export default function DonorNotificationsPage() {
  return <NotificationCenter />;
}
