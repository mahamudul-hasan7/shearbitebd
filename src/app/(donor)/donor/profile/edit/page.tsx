import { ProfileEditor } from "@/features/donor-account/profile-editor";

export const metadata = { title: "Edit Donor Profile | ShareBite BD", description: "Update mock donor identity, organization, and contact information." };

export default function EditDonorProfilePage() {
  return <ProfileEditor />;
}
