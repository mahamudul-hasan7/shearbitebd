import { DonorProfileView } from "@/features/donor-account/donor-profile";

export const metadata = { title: "Donor Profile | ShareBite BD", description: "Review verified donor identity, private contact details, and rescue impact." };

export default function DonorProfilePage() {
  return <DonorProfileView />;
}
