import { DonorSettings } from "@/features/donor-account/donor-settings";

export const metadata = { title: "Donor Settings | ShareBite BD" };

export default function DonorSettingsPage() {
  return <DonorSettings />;
}
