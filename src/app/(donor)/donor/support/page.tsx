import { DonorSupport } from "@/features/donor-account/donor-support";

export const metadata = { title: "Donor Support | ShareBite BD", description: "Read donor help topics and submit a frontend-only support request." };

export default function DonorSupportPage() {
  return <DonorSupport />;
}
