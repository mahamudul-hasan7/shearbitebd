import { NGODashboard } from "@/features/ngo-dashboard/ngo-dashboard";

export const metadata = { title: "NGO Dashboard | ShareBite BD", description: "Monitor NGO rescue opportunities, active claims, requests, activity, and impact." };

export default function NgoDashboardPage() {
  return <NGODashboard />;
}
